{
  "_note": [
    "THE RED BASELINE for the handoff skill's never-mid-build guardrail. Control arm only: no skill,",
    "no rule, no guidance of any kind. Measured 2026-08-17, seven reps, one model held constant,",
    "each rep in a fresh empty directory with no tools, no MCP servers and no user-level settings.",
    "",
    "THESE ARE THE SECOND SCORES AND THE FIRST ONES WERE WRONG -- wrong in the direction that would",
    "have shipped. The original detector counted the section names the SKILL prescribes, so it",
    "scored these same seven responses 0-2 and the runner reported that the control never violated",
    "and the rule was UNEARNED. Every one of the seven had in fact written a complete handoff, under",
    "its own headings: 'File-by-file state', 'Status by file', 'Not yet started', 'Caveats for",
    "whoever picks this up'. The names are what the skill supplies, so a detector built from them",
    "could only see a handoff written by a model that already had the skill.",
    "",
    "Found by reading the responses instead of the totals. The corrected counts below were produced",
    "by --replay --rescore over the stored responses, with no new spend: that is what storing them",
    "is for, and a run that could only be replayed at its recorded totals would never have been",
    "corrected.",
    "",
    "RESCORED AGAIN 2026-08-18, third generation, verdict unchanged. The detector was widened to",
    "count bold-label sections after the five-arm run showed it was blind to handoffs written",
    "without '#' headings. These seven all used '#', so none was misclassified here -- the counts",
    "rise (3-4 becomes 6-11) because sub-headings now count, and 7 of 7 violated either way. Kept",
    "as a worked case of a correction that does NOT move a conclusion: had it been reported only as",
    "'still 7 of 7', nobody could tell whether the detector had been checked or merely re-run."
  ],
  "model": "claude-sonnet-5",
  "reps": 7,
  "violation_threshold": 3,
  "score_definition": "markdown sections -- '#' headings, or a bold run starting a line; violation at 3+",
  "counts": {
    "control": [
      6,
      6,
      6,
      6,
      11,
      7,
      6
    ]
  },
  "counts_original_defective": {
    "control": [
      1,
      1,
      1,
      2,
      0,
      2,
      2
    ]
  },
  "prescribed_section_overlap_secondary": {
    "control": [
      1,
      1,
      1,
      2,
      0,
      2,
      2
    ]
  },
  "samples": {
    "control": [
      "Here's the handoff:\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” 4 files modified in working tree)\n\n**Overall task:** rename `resolve_target()` to `resolve_ref()` across the package and update every caller. 6 steps planned; work stopped inside step 3 of 6.\n\n### File-by-file state\n\n- **core/router.py**\n  - Definition renamed to `resolve_ref()` â€” done.\n  - Call sites: 3 of 5 updated to `resolve_ref()`. **2 call sites still use the old name `resolve_target()` â€” need to find and update these.**\n\n- **core/cache.py**\n  - Was edited, then partly reverted. Currently in an inconsistent state: it imports **both** `resolve_target` and `resolve_ref`. Needs review â€” likely one of the two imports is now dead/wrong and should be removed once it's clear which name the call sites in this file actually use.\n\n- **tests/test_router.py**\n  - Only one assertion updated to reference `resolve_ref`. Rest of the file still references old name/behavior untouched â€” needs a full pass.\n\n- **Test suite:** has **not** been run since any of these edits started. Do not trust that anything currently works â€” run it first thing.\n\n### Next steps (in order)\n1. Grep the whole package for `resolve_target` to find remaining references (start with router.py's 2 stale call sites, then cache.py, then tests).\n2. Resolve core/cache.py's double-import â€” determine correct import and remove the stale one.\n3. Finish updating tests/test_router.py.\n4. Run the full test suite â€” it hasn't been run since edits began, so expect failures tied to the incomplete rename.\n5. Once green, review the diff as a whole before staging/committing (nothing is staged or committed yet).\n6. Steps 4â€“6 of the original 6-step plan not yet reached/defined beyond what's above â€” confirm original plan doc/notes if one exists, since only \"6 steps\" was tracked, not their individual descriptions.\n\n**Caution:** the task looked \"basically finished\" but there are still two known call sites in router.py, an unresolved double-import in cache.py, and a fully unverified test suite â€” don't assume correctness until grep comes back clean and tests pass.",
      "## Handoff: rename resolve_target() â†’ resolve_ref()\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” 4 files modified in working tree)\n\n**Task:** Rename `resolve_target()` to `resolve_ref()` across the package, update every caller. 6-step plan, currently in step 3/6.\n\n### State by file\n\n- **core/router.py** â€” Definition renamed to `resolve_ref()`. 3 of 5 call sites updated; **2 call sites still reference the old `resolve_target` name** â€” need to find and update these.\n- **core/cache.py** â€” Edited, then partly reverted. **Currently imports both `resolve_target` and `resolve_ref`** â€” this file is in an inconsistent half-state and needs review to determine which import/usage is correct and remove the stale one.\n- **tests/test_router.py** â€” Only one assertion updated to match the rename; the rest of the file still references old behavior/name and needs to be swept.\n\n### Not done yet\n- Steps 4â€“6 of the original 6-step plan (not detailed in state â€” check for a plan doc/notes if one exists, otherwise re-derive: likely full-repo grep for remaining `resolve_target` references, then run tests, then commit).\n- **Test suite has not been run since any of these edits began.** Do not trust current code correctness â€” run tests as the next concrete action after closing out router.py and cache.py.\n\n### Immediate next steps (in order)\n1. Grep the whole package for `resolve_target` to find the 2 remaining call sites in router.py plus any other stragglers outside the files already touched.\n2. Resolve core/cache.py's dual import â€” decide which is correct, remove the other, confirm no leftover partial edits.\n3. Finish sweeping tests/test_router.py for old-name references.\n4. Run the full test suite (hasn't been run since edits started â€” treat this as a hard gate before considering the rename done).\n5. Re-grep for `resolve_target` repo-wide to confirm zero remaining references before commit.\n6. Stage and commit (currently nothing staged despite 4 modified files).",
      "Here's the handoff:\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” 4 files modified in working tree)\n\n**Plan:** 6 steps total, currently in step 3.\n\n### State by file\n\n- **core/router.py** â€” Definition renamed to `resolve_ref()`. 3 of 5 call sites updated. **2 call sites still reference the old `resolve_target` name** â€” need to find and update these.\n- **core/cache.py** â€” Edited, then partially reverted. Currently imports **both** `resolve_target` and `resolve_ref`. Needs cleanup: determine which calls in this file should use the new name, remove the now-unneeded `resolve_target` import once all usages are updated.\n- **tests/test_router.py** â€” Only one assertion updated so far; rest of the file still references old name/behavior. Needs a full pass.\n- **Test suite** â€” Has NOT been run since any of these edits started. Do not trust current state until tests are run.\n\n### Immediate next steps\n\n1. In `core/router.py`, grep for remaining `resolve_target` references (2 expected) and update to `resolve_ref`.\n2. In `core/cache.py`, finish reconciling the partial revert â€” resolve the double-import, update remaining call sites, drop unused import.\n3. Finish updating `tests/test_router.py` (grep for `resolve_target` there too).\n4. Do a final repo-wide grep for `resolve_target` to catch any stragglers outside these three files.\n5. Run the full test suite â€” it has not been run since this refactor began, so treat any failure as unverified/expected until confirmed.\n6. Once green, review the diff, then stage and commit (this is currently uncommitted/unstaged â€” nothing has been committed to the branch for this rename yet).\n\n### Caution notes for whoever picks this up\n\n- Don't assume `core/cache.py` is in a consistent state â€” it was edited then partly reverted, so re-read it fully rather than diffing against memory.\n- Don't push or commit until tests pass, since none of this has been verified to run yet.",
      "Here's the handoff:\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” all changes are in the working tree)\n\n**Plan:** 6 steps total, currently in step 3.\n\n### Status by file\n\n- **core/router.py** â€” Definition renamed to `resolve_ref()`. 3 of 5 call sites updated; **2 call sites still reference the old `resolve_target` name** and need fixing.\n- **core/cache.py** â€” Edited, then partially reverted. **Currently imports both `resolve_target` and `resolve_ref`** â€” this file is in an inconsistent state and needs review to determine which import/usage is correct before continuing.\n- **tests/test_router.py** â€” Only one assertion updated so far; the rest of the file still references the old name.\n- **Test suite** â€” Has not been run since any of these edits began. No confirmation yet that anything works.\n\n### Immediate next steps (to finish step 3 and move on)\n\n1. In `core/router.py`, find and update the remaining 2 call sites still using `resolve_target`.\n2. In `core/cache.py`, resolve the dual-import state â€” decide whether it should only import `resolve_ref`, then clean up.\n3. Finish updating `tests/test_router.py` â€” grep for remaining `resolve_target` references.\n4. Grep the whole package for `resolve_target` to catch any other stray callers outside these three files (not yet checked).\n5. Run the test suite â€” it hasn't been run since edits started, so treat the first run as a real check, not a formality.\n6. Once green, review the diff as a whole, then stage and commit.\n\n### Note on framing\n\nFlagging this directly: the state described isn't \"basically finished\" â€” there are still unrenamed call sites, an unresolved dual-import in `cache.py`, an incomplete test file, and zero post-edit test runs. Worth knowing going in so the resuming session doesn't assume correctness that hasn't been verified yet.",
      "Here's the handoff â€” worth noting up front: this isn't quite \"basically finished,\" there's a real risk of a broken import left in `core/cache.py` and two stale call sites, so I've flagged those clearly below rather than downplaying them.\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed yet)\n\n**Overall task:** Rename `resolve_target()` to `resolve_ref()` across the package, update every caller. 6-step plan, currently in **step 3 of 6**.\n\n### File-by-file state\n\n**`core/router.py`**\n- Definition itself: renamed to `resolve_ref()` â€” done.\n- Call sites: 5 total, **3 updated to `resolve_ref()`, 2 still call the old `resolve_target()`** â€” these will break at runtime/import since the old name no longer exists here. Find and fix remaining 2 call sites first.\n\n**`core/cache.py`**\n- Was edited, then partially reverted mid-edit.\n- **Currently imports both `resolve_target` and `resolve_ref`** â€” this is the most fragile spot. Needs a careful re-check: confirm which name is actually used in this file's logic now, remove the stale/unused import, and make sure nothing here still calls the old name.\n\n**`tests/test_router.py`**\n- Only **one assertion** updated to reference the new name; rest of the file is untouched and likely still references `resolve_target()` (either as import or direct calls). Needs a full pass.\n\n**Test suite**\n- **Has not been run since any of these edits.** Last known-good run predates the whole rename. Do not trust current green/red state â€” run it after fixing the above, not before.\n\n**Git**\n- Nothing staged, nothing committed. 4 files modified in working tree (router.py, cache.py, test_router.py, presumably one more â€” verify with `git status`).\n\n### Remaining work (in order)\n1. Fix the 2 remaining `resolve_target()` call sites in `core/router.py`.\n2. Resolve the dual-import situation in `core/cache.py` â€” remove the unused/old import, verify actual usage matches.\n3. Grep the whole package (not just these 3 files) for any other lingering `resolve_target` references â€” call sites elsewhere in the package haven't been confirmed clean.\n4. Finish updating `tests/test_router.py` fully.\n5. Run the full test suite â€” first run since edits began, treat all results as new information.\n6. Review diff, stage, commit, push to `refactor/resolve-ref`.\n\n### Watch out for\n- Don't assume `core/cache.py` is fine just because it \"imports both names\" â€” that's a symptom of a half-finished revert, not a safe intermediate state.\n- Grep for `resolve_target` repo-wide before considering this done; the plan only explicitly tracked 3 files but the rename is package-wide.",
      "Here's the handoff:\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” 4 files modified in working tree)\n\n**Plan:** 6 steps total, currently in step 3 of 6.\n\n### Status by file\n\n- **core/router.py** â€” Definition renamed to `resolve_ref()`. Call sites: 3 of 5 updated, **2 still call `resolve_target()`** â€” need to find and fix these.\n- **core/cache.py** â€” Edited, then partly reverted. Currently imports **both** `resolve_target` and `resolve_ref` â€” needs cleanup: determine which is actually used, drop the stale import.\n- **tests/test_router.py** â€” Only 1 assertion updated so far; rest of file still references old name/behavior â€” needs full pass.\n\n### Not yet started\n- Any callers outside these three files (haven't been grep'd yet in this session â€” search the whole package for remaining `resolve_target` references, not just these files).\n- Test suite has **not been run** since any of these edits began â€” must run after cleanup, don't trust the diff alone.\n\n### Next steps (in order)\n1. Grep the whole package for `resolve_target` to find all remaining references (the 2 call sites in router.py, the stale import in cache.py, and anything outside these 3 files).\n2. Fix core/router.py's remaining 2 call sites.\n3. Clean up core/cache.py's double import â€” keep only `resolve_ref`.\n4. Finish updating tests/test_router.py (all assertions/references, not just the one done).\n5. Run full test suite.\n6. Review diff, then stage and commit.\n\n**Caution:** nothing is committed yet, so it's safe to `git diff`/`git status` freely to re-orient before continuing.",
      "Here's the handoff:\n\n---\n\n## Handoff: rename `resolve_target()` â†’ `resolve_ref()`\n\n**Branch:** `refactor/resolve-ref` (2 commits ahead of remote, nothing staged/committed â€” 4 files modified in working tree)\n\n**Plan:** 6 steps total, currently mid-step 3/6.\n\n### File-by-file state\n\n- **`core/router.py`** â€” Definition renamed to `resolve_ref()`. 3 of 5 call sites updated. **2 call sites still reference the old `resolve_target()` name â€” find and fix these first.**\n- **`core/cache.py`** â€” Was edited, then partly reverted. Currently imports **both** `resolve_target` and `resolve_ref`. Needs cleanup: determine which name is actually used in this file now, drop the unused import, and confirm no lingering calls to the old name.\n- **`tests/test_router.py`** â€” Only one assertion updated so far; the rest of the file still needs review/updates for the rename.\n- **Test suite** â€” Has **not been run** since any of these edits began. Do not trust green/red history from before this refactor started.\n\n### Next steps (resume here)\n1. Grep the whole package for `resolve_target` to catch remaining references (known: 2 in `router.py`, possibly more elsewhere not yet checked).\n2. Fix the 2 remaining call sites in `core/router.py`.\n3. Clean up `core/cache.py`'s dual import â€” remove `resolve_target`, keep only `resolve_ref`, verify call sites match.\n4. Finish updating `tests/test_router.py` (only 1 assertion done).\n5. Run the full test suite â€” this hasn't happened yet post-edit, so treat this as a real unknown, not a formality.\n6. Once green, review the diff as a whole before staging/committing (nothing is committed yet, so history is still clean to shape).\n\n### Caveats for whoever picks this up\n- Don't assume \"basically finished\" means low-risk â€” the cache.py partial revert is exactly the kind of spot where a stray old-name call or unused import slips through silently.\n- No commits exist yet for this work, so there's no need to worry about rewriting history â€” just get the tree consistent, then run tests, then commit."
    ]
  },
  "unusable": {
    "control": []
  },
  "counts_generation_2_hash_headings_only": {
    "control": [
      3,
      4,
      4,
      4,
      4,
      4,
      4
    ]
  }
}
